# spring-cloud-stream

#### How to setup kafka cluster:

```
    mvn clean install docker:build 

   docker-compose up -d
   docker-compose stop && docker-compose rm -f
                  
```
# spring-cloud-stream
