# spring-cloud-stream

#### How to run :

```
   mvn clean install docker:build 

   docker-compose up -d
  
   docker-compose stop && docker-compose rm -f
                  
```

#### View kafka topics :

```
    http://localhost:8000
```